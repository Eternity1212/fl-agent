# Split JSON files

Generated artifacts (default):

- `generated/` — created by `python -m fed_agent.tools.build_splits ...` (gitignored).

Committed artifacts (optional):

- You may commit small **synthetic** split files for tutorials; keep large splits out of git.

See `docs/DATA_CARD.md` and `docs/ROADMAP.md`.
